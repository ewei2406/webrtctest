name: Deploy to GitHub Pages

on:
  pull_request:
    branches:
      - master
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR Number (for manual deploys)'
        required: false
        default: '0'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm i

      - name: Build project
        run: npm run build

      - name: Determine PR Number
        id: pr_number
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "::set-output name=pr_number::${{ github.event.pull_request.number }}"
          else
            echo "::set-output name=pr_number::${{ github.event.inputs.pr_number }}"
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          target_branch: pr-${{ steps.pr_number.outputs.pr_number }}
          cname: "webrtc-dev-${{ steps.pr_number.outputs.pr_number }}.edwardwei.com"
